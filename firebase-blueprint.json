{
  "entities": {
    "CardData": {
      "title": "Card Data",
      "description": "Stored credit card numbers from admin dashboard",
      "type": "object",
      "properties": {
        "content": {
          "type": "string",
          "description": "The raw text content of the card numbers"
        },
        "cardNumbers": {
          "type": "array",
          "description": "List of valid card numbers"
        },
        "cards": {
          "type": "array",
          "description": "List of valid card numbers (compatibility field)"
        },
        "numbers": {
          "type": "array",
          "description": "List of valid card numbers (compatibility field)"
        },
        "updatedAt": {
          "type": "string",
          "format": "date-time",
          "description": "Last update timestamp"
        }
      },
      "required": ["content", "updatedAt"]
    }
  },
  "firestore": {
    "/system/vault": {
      "schema": "CardData",
      "description": "Single document storing all card data"
    }
  }
}
